import { checkForQuestion } from '../client/js/questionChecker';

describe('sentenceChecker', () => {
  test('checkForSentence should be defined', () => {
    expect(checkForQuestion).toBeDefined();
  });
});

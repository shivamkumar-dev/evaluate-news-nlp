import { checkForSentence } from '../client/js/sentenceChecker';

describe('sentenceChecker', () => {
  test('checkForSentence should be defined', () => {
    expect(checkForSentence).toBeDefined();
  });
});
